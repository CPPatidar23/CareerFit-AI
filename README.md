# CareerFit

AI-powered career recommendation using TF-IDF + Logistic Regression and a rule-based expert system.

## Run
```bash
pip install -r requirements.txt
python train_model.py
streamlit run app.py
```

The 50,000-row dataset is synthetic academic data; do not describe it as real survey data.
